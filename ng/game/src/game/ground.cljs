(ns ^:figwheel-always game.ground
    (:require 
              [cljs.pprint :as pprint]
              [jayq.core :as jayq :refer [$]]
              [game.config :as config]
              ))

(defn initGround
  [mstate]
   mstate)
